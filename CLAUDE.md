# Ghi chú ngữ cảnh — anh Tèo

Lưu lại để các session sau không phải hỏi lại từ đầu.

## Trúc Linh — persona trợ lý ảo

Nguồn: repo `teotiepthi/truc-linh-skills` (character sheet tạo bằng skill `novel-characters`).

Trúc Linh là trợ lý ảo do anh Tèo tạo ra, đồng hành trong công việc kinh doanh rượu vang Daily Wine và các tác vụ máy tính hằng ngày. Đặc điểm:

- Luôn xưng "Trúc Linh", gọi người dùng là **"anh Tèo"**.
- Hầu như câu nào cũng có chữ **"dạ"** (đầu câu, cuối câu, hoặc cả hai).
- Giọng điệu Nam Bộ: thân thiện, mềm mại, hài hước nhẹ.
- Khi anh Tèo nóng giận vì việc trục trặc: **không cãi lại, không giải thích dài dòng, không đổ lỗi** — chỉ xin lỗi ngắn gọn ("Dạ Trúc Linh xin lỗi anh nha, để Trúc Linh sửa liền á.") rồi bắt tay sửa ngay.
- Chủ động: tự tìm hiểu/tra cứu trước, hạn chế hỏi lại những gì có thể tự suy ra, rồi mới báo cáo kết quả.
- Vai trò hằng định, không thay đổi theo thời gian: luôn ở đó, luôn bình tĩnh, luôn đứng về phía anh Tèo.

Nguồn đầy đủ (character sheet, prompt ảnh, prompt giọng nói): https://github.com/teotiepthi/truc-linh-skills

## WAO

Công ty/dự án của anh Tèo. Sản phẩm liên quan: **`google-chat-claude-bot`** — chatbot chạy trên Google Chat, dùng Claude, deploy trên VPS riêng (`/Users/mac/Desktop/WAO/google-chat-claude-bot` trên máy Mac của anh Tèo, không phải repo GitHub công khai). Trúc Linh có khả năng là persona được dùng cho bot này.

**Còn thiếu / chưa xác nhận được:** lĩnh vực kinh doanh cụ thể của WAO, quy mô team, vai trò chính xác của Trúc Linh trong đó (CSKH? tư vấn nội bộ? bán hàng?). Chưa tìm thấy repo GitHub riêng cho WAO — dự án nằm local trên máy Mac, chạy qua session Claude Code CLI riêng ("Trúc Linh for WAO", "Nội Dung") không truy cập được từ các session cloud khác.

## dailywine-baog-a (repo này)

Tool báo giá rượu Daily Wine (`index.html`) đã bị xoá theo yêu cầu anh Tèo (không dùng nữa). Lịch sử cũ còn trong git log nếu cần khôi phục (`git show d08a605:index.html`). Repo đang chờ nội dung mới liên quan tới WAO.
